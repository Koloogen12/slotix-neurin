# Slotix — Лендинг · что изменилось

Все правки в одном файле: **`Slotix - Лендинг.dc.html`**. Ниже — что добавить/поменять на боевом лендинге.

---

## 1. Новая секция «Почему Slotix, а не просто календарь»

Расположение: между блоком с календарём («Вы управляете расписанием…») и секцией «Интеграции». Якорь `id="why"`.

Структура:
- **Шапка секции**: eyebrow «Почему Slotix, а не просто календарь», H2 «Встреча не заканчивается, когда вы кладёте трубку», подзаголовок.
- **Блок 1 (главный, крупная карта на всю ширину)** — AI-конспект. Слева текст + CTA **«Забронировать ранний доступ»** (сейчас якорь на `#pricing`), справа мокап карточки конспекта с встроенной петлёй пересозвона («Созвониться через 2 недели» → кнопка «Записаться»).
- **Блоки 2–5 (равные карточки, grid auto-fit)**: петля пересозвона, Telegram-напоминания, пакеты встреч, российские интеграции.

Тексты — финальные, в файле. Порядок карточек менять нельзя (по убыванию уникальности).

Статус фич: **бейджей «Скоро» нет** — всё идёт сразу в релиз (заказчик подтвердил).

> ⚠️ CTA «Забронировать ранний доступ» сейчас скроллит к общей форме email в `#pricing`. Если для ранних лидов нужна отдельная воронка/waitlist — это ещё не сделано, нужен отдельный эндпоинт.

## 2. Модалка входа (новое)

Открывается по клику на **«Вход»** (шапка) и **«Войти»** (футер) — оба теперь не ссылки, а триггеры модалки (`openLogin`). Отдельной страницы входа нет, только модалка. Регистрация по-прежнему ведёт на страницу онбординга (`Slotix - Онбординг.dc.html`).

Три состояния (state `authView`):
- `login` — Google-вход + email/пароль + «Забыли пароль?». Кнопка «Войти» → `Slotix - Кабинет.dc.html`.
- `reset` — email + «Отправить ссылку» + возврат ко входу.
- `sent` — экран «Проверьте почту».

Закрытие: крестик или клик по фону; при закрытии состояние сбрасывается на `login`.

Формы **без реальной логики** — это прототип UI. Нужно подключить: авторизацию (email/пароль + OAuth Google), реальную отправку письма восстановления, валидацию и ошибки.

## 3. Футер

- Копирайт: было «© 2020–2026 Slotix» → стало **«© 2026 Slotix»**.
- Убрана ссылка **«Тарифы»** (колонка «Продукт»).
- Убрана ссылка **«Сообщество»** (колонка «Поддержка»).
- Убран **блок выбора языка** целиком (только русский).

## 4. Копирайт-правка

Блок 4 в новой секции: заголовок «Продавайте **10 занятий**, а не одну встречу» → «Продавайте **встречи сразу пакетом**».

---

### Зависимости файла
`support.js`, `image-slot.js`, папка `assets/` (`aurora-bg.png`, `slotix-logo.png`, `slotix-icon.png`).
